# Supabase (optional during local development — see docs/adr/0023-web-app-source-repo-doctrine.md)
# Copy to .env and fill real values ONLY in a local, untracked .env file.
# Never commit real values (GIT-AS-5).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=