# Changelog — Cargo Control

> Root repository surface file per ADR 0021 (§4 Repository surface) +
> DOC-2 (one home per topic). **THE home** for the human-facing change
> log. This file records visible, user-facing changes, newest first,
> per fase. It is NOT a duplicate of the internal decision provenance:
> the detailed trail lives in `DECISION LOG.md` (every Record) and the
> ADRs under `docs/adr/`. This home only summarizes what a reader can
> see externally.

## Fase 21 — Complete documentation

- Added a complete documentation doctrine for the repository:
  ADR 0020 (documentation doctrine), the architecture documentation
  home, the domain home README, and the documentation QA asserts
  (DOC-AS-*) with their QA home.
- Added the professional GitHub development doctrine:
  ADR 0021 (GitHub development doctrine) and the architecture
  GitHub workflow home.

## Fase 20 — Professional UX / responsive

- Added professional UX doctrine homes and responsive/mobile QA asserts.
  (Historically recorded in `DECISION LOG.md` Records; newest-visible
  summary continues below.)

## Earlier fases

- See `docs/domain/README.md` (Cargo Control product, domain, brand,
  UX, and architecture doctrine homes) and `DECISION LOG.md` for the
  complete per-Record trail (Records 1..21) of every fase.

> Doctrine: one home per topic (DOC-2). The detailed provenance for any
> phase is `DECISION LOG.md` + `docs/adr/*`; this file is the concise
> human-facing summary and never restates the doctrine (DOC-3).
